const styles = (theme) => ({
	chatListContainer: {
		backgroundColor: theme.palette.background.paper,
		height: 'calc(100vh - 35px)',
		left: '0',
		width: '300px',
	},
	listItem: {
		cursor: 'pointer',
	},
	newChatBtn: {
		borderRadius: '0px',
	},
});

export default styles;
